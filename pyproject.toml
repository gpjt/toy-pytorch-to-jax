[project]
name = "toy-pytorch-to-jax"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "jax>=0.10.1",
    "numpy>=2.4.6",
    "torch>=2.12.0",
]
